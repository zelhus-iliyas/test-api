from django.urls import path
from . import views

urlpatterns = [
    # path('events/', views.RetrieveEventsAPIView.as_view(), name='events-list'),
    # path('lists/', views.RetrieveCalendarListsAPIView.as_view(), name='calendars-list'),
]
